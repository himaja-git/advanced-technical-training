arr=list(map(int,input().split()))
k=int(input())
n=len(arr)
left=0
right=k-1
Sum=sum(arr[left:right+1])
maxSum=Sum
while(right<n-1):
    Sum-=arr[left]
    left+=1
    right+=1
    Sum+=arr[right]
    maxSum=max(maxSum,Sum)
print(maxSum)
